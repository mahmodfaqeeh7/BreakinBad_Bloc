import 'package:flutter/material.dart';

class Chardetails extends StatelessWidget {
  const Chardetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
